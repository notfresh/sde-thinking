---
title: "开源参与问题：CodingAgent代码太多而你甚至自己没细看代码！"
date: 2026-09-07
series: SDE-Thinking
number: 012
---

# 开源参与问题：CodingAgent代码太多而你甚至自己没细看代码！

## 一、现象：六个画面

最近三个月，你有没有在这些画面里待过？

凌晨两点，agent 跑完了。屏幕上多了一千多行 diff，你自己都数不清有几个文件。注释是有的，但注释里写的"这是一个简单的辅助函数"——你看着都不信。点提交。

早上醒来，你做了"5 分钟自审"。其实没读懂逻辑，只是扫了一眼——agent 写得"看起来对"，命名也工整，函数也不长。但你心里清楚，如果 reviewer 追问一句"为什么这里用 mutex 不用 channel"，你答不上来。

标题写得很长，body 写满 motivation、implementation、test plan。push 之前你甚至没注意到一个 typo 在最显眼的位置。满怀信心，按下按钮。

然后是维护者的沉默。一周、两周、没有任何评论。你开始焦虑——是不是我哪里写错了？是不是 maintainer 太忙？是不是这个项目根本不看外部 PR？无数种解释在你脑子里翻，但你没想过最朴素的一种：你提的那个 PR，维护者根本不知道从哪儿读起。

维护者终于回了一句话——"能不能拆一下？"六个字。你知道这意味着什么：他不是不想 review，是要 review 的东西太多，只能给你最简的反馈。你以为他偷懒，其实他已经在所有 PR 里给最简反馈。

再过一个月，你看到自己那个 PR 被自动 close 了。标签写着 `stale`。你这才意识到——这个仓库里曾经属于你的那个位置，已经被时代冲刷干净了。

这六个画面，是 agent 时代每个贡献者的出厂设置。

**这不是代码写得太快的问题。**

## 二、拆穿：投稿不是出版

你给出版社投稿的那一刻，不叫"出版"。

出版社决定用你稿子，是审稿通过；印出来摆上书架，是出版；有人读，有人引用，那是十年后的事。整个链条上，"我提交了"只是一次投稿——中途撤稿、出版社拒收、印了卖不动，**责任都是你的**。

agent 时代的 PR 也是这个结构。你按下回车提交的那一刻，只是把稿子投了出去。后续每一个环节——reviewer 看完、maintainer 决定合、合并后跑生产、被真实用户使用——都还在你身上。一个 PR 被合并进 main，不意味着你贡献完了；它意味着**你的责任刚刚开始**。

你以为"写完"是终点，其实是起点。

## 三、反转：合并不是终点，质量责任在写代码的那一刻就锁定了

如果贡献的起点是合并，那责任锁定在合并的那一刻——这听起来很公平，对吧？

**错了。**

质量责任的锁定时刻，比合并早得多。它锁定在你按下 agent 运行键的那一刻。

agent 输出的 1000 行代码**进入你的 PR** 的那一刹那，质量责任就落在了**你**身上——不是 agent，不是 reviewer，不是你用的那个 LLM 厂商。你。它是你的选择，你按了 Enter。

合并与否，只是责任是否被发现。

人写代码的时候，至少还"边写边懂"——哪怕最后忘了细节，写的过程里他**摸过每一行**。agent 写代码的时候，你从头到尾可能一行都没读过。**责任和代码一起进了 main**——进了就进了，不能撤回，不能悔棋。

更隐蔽的代价，是**暗物质**。agent 写的代码进了 main，你没看懂的那部分就成了仓库里的暗物质。它在那里、它在影响下游、它会在三个月后某个深夜被另一个人当作前提假设引用——而你不知道它是什么。

合并不是质量责任的起点，也不是终点。**按下 Enter 那一刻，责任就锁定了。**合并只是让责任被看见。

## 四、立论：不能因为用了 Agent 写代码反而自己不看代码，这样做是不负责任的

没有人在逼你用 agent。没有人。

你完全可以不用——手写、慢、累、写出 bug、被 reviewer 骂，但那是你**自己读过每一行**的代码。你也可以用——快、多、轻松，但你**有义务**承担 agent 输出带来的额外责任。这两件事是同一个选择的两个面。

用 agent 是你的选择。

没读懂也是你的选择。

**不能因为用了 Agent 写代码反而自己不看代码，这样做是不负责任的。**

不是 agent 不负责任。是**你不负责任**——把 agent 的输出当成了自己的贡献。reviewer 没读懂你的代码是 reviewer 的能力问题，但**你自己都没读过，那是你的责任**。

把"我用 agent"当挡箭牌是没用的。agent 不会出现在 PR 的署名里，仓库的 git log 上写的是你的名字。

## 五、实操：三件事让 agent 不替你死掉

写到这里你可能会说："行行行，我知道责任在我了，给我操作清单。"好，下面是三件事。每件事都是一个动作，做了就比不做强。

### 第一件：三档阅读法——带着目的读 agent 写的代码

不是"逐行读完"。那是反人类的、不必要的、也是错的。

读 agent 代码，只做三档：

- **第一档：意图（30 秒，必读）**——这段代码要解决什么问题？放在这个文件的哪个位置？和它相邻的代码是什么关系？
- **第二档：架构（3-5 分钟，必读）**——主流程怎么走？哪个函数是入口？边界条件在哪？引入了哪些新的依赖？
- **第三档：逐行精读（选读，只对关键路径）**——修改核心逻辑、错误处理、安全边界、性能热点时逐行读；其他放过。

**核心原则：不是"读不读"，而是"在哪一层停下来才算负责任"。**

如果你能把 1000 行 PR 用"三档"读完，你已经做了 90% 该做的事。剩下 10% 是留给 reviewer 的——别把所有负担都揽在自己身上，但**自己那份必须读完**。

### 第二件：让 agent 自审——风格是机械的事，机械的事让 agent 干

风格对齐是机械活——命名规范、缩进、import 顺序、类型注解、错误处理模板。这些**不需要你的判断**，需要的是一致性。

第一轮 prompt 让 agent 写功能。第二轮 prompt：

> "按本仓库 CONTRIBUTING.md / 既有代码风格 / lint 规则 / 类型注解习惯，自审一次并重写。把违规部分标出来说明原因。"

这一轮不是让你手动改代码——是让 agent 干它擅长的机械活，把你的注意力从"盯着分号空几格"里腾出来，集中读"意图+架构"。

如果你连这轮都不做，那风格合规的责任事实上落到了维护者头上——可他们没义务帮你整理你自己 agent 的代码。

### 第三件：拆分粒度——别让 agent 把一个 PR 写成一个哥布林

agent 默认输出是"功能级"的——一个 prompt 出一个完整功能，1000 行起步。但 PR 不是功能级，是**可审级**。

一条经验：单个 PR **不超过 200 行有效改动**。超过就拆。拆的依据不是"按文件拆"或"按函数拆"，而是**按"reviewer 读完不累"的粒度拆**。

拆完之后，单个 PR 仍然是完整的、可独立合、独立回滚、独立 review。每个小 PR 仍然要过三档阅读——只是工作量从"读 1000 行"变成"读 150 行 × 6 次"。

agent 输出 1000 行 ≠ 你必须提一个 1000 行的 PR。**agent 是工具，不是 PR 形状的来源**。

---

如果你只做这三件事，你的 PR 质量就已经超过 80% 的 agent 时代贡献者。

剩下两件事点到为止：

- **责任自负清单**——合并前必须做的三件事：① 读完三档 ② 跑一遍测试 ③ 看一遍完整 diff。缺一不可。
- **PR 礼仪**——标题短、body 完整、changelog 写清楚、附 issue 链接。这不是客套，是减少 reviewer 心智负担的最直接动作。

## 六、维护者端：PR 洪流下，流程怎么不被打死

上面三件事是贡献者视角。但 PR/issue 积压不是单一贡献者能解决的——**维护者端也要改造流程**，否则再优秀的贡献者也救不了一个被堵死的仓库。

三条建议：

**第一，前过滤比后审查重要。** reviewer 是稀缺资源。能在 review 之前用 bot / CI / 模板挡掉的，不要等 reviewer 来挡。lint 不过、测试没过、PR 描述不写、超过 500 行——自动打回或自动加标签。

**第二，PR 不是越多越好。** 维护者的真实成本在 review，不在合并。一个项目一周涌入 800 个 PR、维护者只来得及合 30 个——这是设计问题，不是人手问题。解决思路：降低合并门槛（CONTRIBUTING 清晰）+ 提高 PR 门槛（拆细、跑通、文档齐）。

**第三，stale 是设计，不是惩罚。** 一个 PR 30 天没动静，bot 自动 close + 提示"re-open 通道"。这是为了不让维护者的注意力被淹没，不是为了惩罚贡献者。如果你的 PR 被 stale 了，重新按本篇三件事过一遍，再开。

**维护者侧的本质转变：评审从"挑毛病"变成"挑值得看的"。** 在 agent 时代，PR 数量爆炸，最稀缺的不是 review 的仔细程度，而是**值得 review 的 PR 的比例**。

## 七、收束：你贡献的不是 commit，是"代码被合并后还活着"

你按下 Enter 那一刻，git 上多了一个 commit。这不是贡献。

贡献是：**3 个月后这个 commit 还在不在 main 上**。是不是有人引用过它的接口？是不是有人在它基础上改过 bug？是不是有一天它要被重构时，你还能看懂当初为什么这么写？

如果回答是"不知道"，那这不是贡献。

agent 时代，贡献的真正单位不是 commit，不是 PR，甚至不是"被合并"——是**代码被合并后还活着的天数**。

你写代码是为了让仓库活下去，还是为了让 git log 上多一行你的名字？

如果是前者，按本文三件事做。如果你想清楚了后者——那你也想清楚了：从此你和这个仓库没什么关系了，commit 进了 main 也不再代表你贡献过什么。

---

**回到最初那六个画面**——凌晨提交、5 分钟自审、维护者沉默、PR 被 stale——它们不是六个独立的悲剧。它们是同一个动作的六个角度：**你按了 Enter，但没承担 Enter 之后的事。**

用 agent 是你的选择。

没读懂也是你的选择。

**你贡献的，不是 commit，是代码活下去的能力——这个能力，必须由你承担。**