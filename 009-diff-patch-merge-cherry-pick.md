# 一份改动的旅程：从 diff 到 cherry-pick

> 软件开发者的日常，一半时间在改代码，另一半时间在跟 git 搏斗。这篇文章把 git 最核心的心智模型讲透：diff、patch、apply、merge、cherry-pick 是怎么从 1974 年的一个 Unix 工具，演化成今天全世界软件协作的骨架的。

## 开场：git 世界里只有快照

忘掉"版本控制"这个词的直觉，先接受 git 最反直觉的事实：**git 仓库里存的是完整快照，不是增量**。你每次 `commit`，它把当时所有文件的内容完整拷贝一份存下来。工作区、暂存区、HEAD 三个区域，就是三个"时刻"的快照：

```
HEAD（最旧） ←commit— 暂存区 ←add— 工作区（最新）
```

git 的所有操作，要么是"复制快照"，要么是"对比快照"。**diff 不是 git 发明的，它是快照对比的产物——而这份产物，自己走过了四十多年的演化史。**

## 第一站：diff，算差异（1974 年诞生）

1974 年，贝尔实验室的 Douglas McIlroy 和 James Hunt 给 Unix 第 5 版写下了 `diff` 命令——它的功能至今没变：**比较两份文件，输出差异**。你可以把它理解成"红笔批改"：不重抄全文，只标哪里改了。

1986 年，Eugene Myers 发表了那个著名的 Myers 算法（把找差异转化为找最长公共子序列），让 diff 又快又准，这个算法至今仍是 git 的默认引擎。

**关键认知：diff 只是"算"出来的，仓库里不存它**。git 存的是两份快照，`+`/`-` 是你执行 diff 命令那一刻现场算出来的。快照是本体，diff 是衍生品。

## 第二站：patch，差异的载体（1985 年）

diff 算出了差异，但"差异"要能**被传递**才有价值。1985 年，Larry Wall（对，就是 Perl 那个 Larry Wall）写了 `patch` 工具，把 diff 的输出存成文件、发出去、对方应用——**"diff + patch" 从此成为软件协作的通用协议**。

Patch 格式经历了三代优胜劣汰：

| 格式 | 年代 | 特点 | 结局 |
|---|---|---|---|
| normal | 1974 | 只有行号和 `<`/`>`，无上下文 | 已淘汰 |
| context | 1985 | 带上下文锚点，但旧块新块各写一遍，体积翻倍 | 被取代 |
| **unified** | **1990** | 上下文保留、冗余砍半 | **今天的事实标准** |

1990 年 8 月，Wayne Davison 提出 unified 格式，一个月后 GNU diff 吸收它。unified 赢在两点：**有上下文锚点**（应用时靠没改的行定位，不靠行号）+ **信息量不减体积减半**。git、GitHub、所有现代工具，全用它。

## 第三站：apply，按锚点写差异

patch 文件到了接收方手里，`git apply` 照着说明书干活：**找上下文（那几行没变的行做锚点），删 `-` 行、插 `+` 行**。找不到锚点就报错拒绝——绝不乱改。

所以 apply 有个先天局限：它只认识两份文件（patch + 当前文件），锚点对不上就死。真实世界里，接收方往往已经改过文件——这就要请出更聪明的机制。

## 第四站：merge，三方谈判

merge 家族（merge、cherry-pick、rebase）的本质是 **3-way merge（三方合并）**。它认识**三份**文件：

```
base（共同祖先）    ← 谈判的基准坐标系
ours（当前分支）    ← 你这边
theirs（要合入的）  ← 对方那边
```

规则只有一条：**以 base 为参照，某行只有一边改了就听那边的；两边都改了同一区域——冲突，请人来裁决**。

这就是为什么 merge 比 apply 强：apply 遇到"上下文不存在"直接死；merge 靠 base 坐标系自动重新定位，只在"两边改同一块"时才举旗。

## 第五站：cherry-pick，摘樱桃

cherry-pick 是 merge 家族里的"精确制导"：**从别的分支摘一个提交过来**。

```
git cherry-pick featureX
```

执行时三步：找到提交 → 算出它相对**亲爹**的 diff（`featureX~1 → featureX`）→ 以三方合并方式应用到当前分支。

**最容易踩的误区**：`cherry-pick featureX` 只摘**一个**提交（分支尖端的那个），带的是一份 diff——"这个提交相对它爸爸改了什么"。连着摘两个提交要用区间 `git cherry-pick b..c`。而且它也会失败：如果被摘的改动和目标分支撞在同一区域，三方合并照样冲突，需要你手工裁决。

## 第六站：省略版与完整版（diff 的最后一层窗户纸）

`git diff A B` 是通用格式，完整版只有**一种形态**：

```
git diff 旧 新
```

所有省略版，都是 git 帮你填了默认参数。

| 命令 | 等价完整版 | 回答的问题 |
|---|---|---|
| `git diff` | `git diff 暂存区 工作区` | 改了还没 add 的 |
| `git diff --staged` | `git diff HEAD 暂存区` | add 了还没 commit 的 |
| `git diff HEAD` | `git diff HEAD 工作区` | 所有没提交的改动 |
| `git diff <commit>` | `git diff <commit> 工作区` | 某提交之后我改了什么 |

**默认参数规则**（最容易绕晕的点）：

- 默认"新"侧 = **工作区**——git 假设你想看"我手头的最新状态"
- 无 `--staged` 时，默认"旧"侧 = **暂存区**
- `--staged` 不是"只改一侧"，而是**把比较窗口整体往历史方向挪一格**：
  - "新"侧：工作区 → 暂存区
  - "旧"侧：暂存区 → HEAD
  - 所以它**同时**改了新旧两侧——这就是"终点变暂存区"和"起点变 HEAD"两个说法都对的原因，它们是一枚硬币的两面
- 显式给一个 commit：只填"旧"侧，新侧仍是默认的工作区

回到三区时间轴就一目了然：

```
HEAD（最旧） ←commit— 暂存区 ←add— 工作区（最新）
```

diff 就是拿时间轴上任意两个时刻对比，`+` 永远指向更新的那侧。`--staged` 可记为"**看暂存区**"——git 自动配对它的旧版（HEAD）和新版（工作区），比较窗口只覆盖 HEAD→暂存区。

## 尾声：一条演化链

把时间线串起来看，这就是一部"改动如何长距离传输"的演化史：

- **1974**：diff 诞生（算差异）
- **1985**：patch 诞生（传差异，context 格式）
- **1990**：unified 格式（传得更好）
- **2002**：Linux 内核用 BitKeeper 管补丁，之前靠邮件手工收 patch
- **2005**：BitKeeper 收回免费许可，Linus 两周写出 git——git 生来就带着邮件基因（`format-patch`/`am`）
- **2008**：GitHub 上线，把"patch + 审核 + 合并"搬上网页，就是今天的 Pull Request

**PR 的本质 = patch 流水线的 web 化**。你点了"Merge"按钮，浏览器背后跑的就是一场三方合并。

一条主线贯穿始终：**diff 算差异 → patch 传差异 → apply 按锚点写差异 → merge 三方谈判 → cherry-pick 摘单颗樱桃**。四十多年前贝尔实验室那个"比较两个文件的工具"，演化成了今天全世界软件协作的骨架。

## 思考题

GitHub PR 页面显示的 diff，是"谁对谁的 diff"？（提示：想想 PR 合并那一刻，三方是哪三方。）
