# 软件为什么复杂（第一篇）：原理简单是压缩，软件复杂是展开

软件越来越复杂，而单个原理往往并不复杂，这其实不是矛盾，而是两个层面的事：
- **原理是"理想模型"**
- **软件是"现实世界中的工程产物"**

一个原理，比如"把用户输入存起来再显示"，核心就是：读入 → 存储 → 输出。这个逻辑几句话就能讲清。

但真正做成一个软件时，它会迅速膨胀。

---

## 1. 原理解决的是"主干问题"，软件要处理的是"所有分支问题"

单个原理通常只描述最核心的路径：
> 用户提交订单 → 扣库存 → 生成订单

这很简单。

但现实中你要考虑：
- 用户重复点击怎么办？
- 库存刚好被抢完怎么办？
- 支付成功但订单没生成怎么办？
- 用户地址有特殊字符怎么办？
- 优惠券能不能叠加？
- 风控判断这是不是刷单？
- 日志要记哪些字段，方便以后排查？
- 接口超时怎么办？
- 数据库写入失败怎么办？

每一个分支看起来都不难，但成百上千个分支组合在一起，软件就复杂了。

所以：
> **单个原理简单，是因为它忽略了现实中的大量例外。**

---

## 2. 软件复杂不是"某一个原理复杂"，而是"大量简单原理叠加"

软件系统通常不是由一个大原理构成的，而是由几百、几千个简单原理叠加而成。

比如一个登录功能：
- 用户输入账号密码
- 密码要加密
- 要防暴力破解
- 要支持手机验证码
- 要支持第三方登录
- 要兼容旧版本客户端
- 要记录登录设备
- 要支持异地登录提醒
- 要处理 token 过期
- 要和企业 SSO 对接

每一个点都不难，单独拿出来都能讲清楚。

但当它们同时存在于一个系统里，复杂就产生了。

这就像：
> 一粒沙子很简单，但一堆沙子形成的沙丘行为就很难预测。

---

## 3. 软件承载的是"历史、组织和人的复杂性"

软件之所以越来越复杂，很多时候不是因为技术本身，而是因为：

### ① 需求不断变化

一开始只想要一个博客，后来要加评论、点赞、广告、会员、审核、国际化、个性化推荐……

每个新需求都可能破坏原来的简单结构。

### ② 历史包袱

早期设计可能很简洁，但随着业务发展，原来的模型已经不适合了。

但系统已经上线，数据已经存在，用户已经依赖，不能随便推翻。

于是只能在旧结构上打补丁，补丁越来越多。

### ③ 多人协作

一个人写的代码可以很简单。

但几十人、几百人同时开发一个系统时，每个人的理解不同、风格不同、目标不同。

系统不再是某个人脑中清晰的模型，而是一个不断演化的集体产物。

### ④ 组织架构影响

康威定律说：
> 软件架构会反映开发它的组织的沟通结构。

部门之间沟通复杂，软件接口也会变复杂。

---

## 4. 原理是"压缩后的知识"，软件是"展开后的现实"

我们觉得原理简单，是因为大脑已经自动忽略了大量细节。

比如"飞机能飞"的原理是伯努利定律：流速快，压强小。

但真正的飞机要考虑材料、燃油、控制系统、安全冗余、乘客舒适度、航空法规……

软件也一样：
> **原理是经过抽象和压缩的，软件则是把所有被压缩的细节重新展开。**

---

## 5. 软件复杂，很多是"偶然复杂性"

软件复杂性通常分两种：

### ① 本质复杂性

问题本身就很复杂，比如天气预报、电商推荐、操作系统调度。

### ② 偶然复杂性

问题本身不复杂，但实现过程中因为技术选型、历史原因、框架限制、性能优化、兼容性等产生的复杂。

很多软件的"越来越复杂"，其实大量是偶然复杂性。

比如：
- 一个简单功能，为了支持十年前的浏览器，写了很多兼容代码
- 一个查询，为了性能，引入了缓存、索引、读写分离、分库分表
- 一个系统，为了对接多个外部平台，写了几十个适配器

这些都不是原理复杂，而是环境复杂。

---

## 6. 软件是"活的"，会不断生长

软件不是一次性完成的，它会持续演化。

刚发布时可能很简洁。

但随着用户增多、功能增多、边界情况增多，它会像城市一样不断扩张。

城市里单条路、单个建筑都不复杂，但整座城市的交通、水电、人口流动、规划管理就非常复杂。

软件也是这样：
> 单个功能原理简单，但系统的复杂性来自数量、连接和演化。

---

## 7. 为什么我们会觉得"原理不复杂"？

因为我们通常是在"事后"理解原理。

当你已经知道答案，回头看，会觉得：
> "这不就是把数据存下来再读出来吗？"

但实际上，在软件还没有成型之前，有无数种选择、无数条岔路、无数个未知问题。

所谓"原理简单"，往往是：
- 忽略了错误处理
- 忽略了性能问题
- 忽略了并发问题
- 忽略了安全风险
- 忽略了历史兼容
- 忽略了人的协作成本

所以：
> 原理简单，是因为我们只看到了被提炼后的主干。
> 软件复杂，是因为它必须活在真实世界。

---

## 一句话总结

**单个原理不复杂，是因为它是理想化、压缩后的模型；软件越来越复杂，是因为它必须在现实中处理大量细节、例外、变化、协作和演化。**

所以软件工程的核心，不是在追求某个"高深原理"，而是在管理复杂性。
